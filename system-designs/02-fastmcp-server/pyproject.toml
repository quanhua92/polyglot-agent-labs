[project]
name = "fastmcp-server"
version = "0.1.0"
description = "FastMCP server implementing add tool"
requires-python = ">=3.12"
dependencies = ["fastmcp", "uvicorn", "requests"]

[project.scripts]
fastmcp-server = "server:main"
