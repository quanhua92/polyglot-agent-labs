[project]
name = "faq-client"
version = "0.1.0"
description = "Test clients for the FAQ API Gateway"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "httpx>=0.27.0",
]

[project.scripts]
test = "test_client:main"
